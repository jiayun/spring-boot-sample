spring:
  profiles:
    active: ${NODE_ENV:dev}
  datasource:
    url: jdbc:mysql://localhost/test
    username: test
    password: test
    hikari:
      maximumPoolSize: 1
  data:
    mongodb:
      uri: mongodb://localhost/test
